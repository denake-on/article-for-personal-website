# 欢迎使用 Markdown

这是一个示例文档。你可以在 `frontend/assets/articles/` 中添加更多 `.md` 文件，
并在 `index.json` 中登记它们以便前端目录加载。

## 要点

- 支持基础的 Markdown 语法（标题、列表、代码块等）
- 支持代码高亮（如需，可后续接入 highlight.js）
- 目录点击后即时加载并渲染

```js
console.log('Hello Markdown');
```


