### 一、FunSearch的基本结构和功能
![Pasted image 20260401211258.png](./assets/media/images/r1_1.png)
**specification**
针对某一个问题，我们现有一个初始的baseline程序，也就是specification的部分，提供了程序的基础接口，而函数进化的关键部分则交给LLM，由于LLM将主要的计算资源集中在进化部分上，因此能够提高整体的效果。
**Prompt编写与岛屿进化**
prompt的编写采用一种拼接的方式，先从程序库中选择K个程序作为父程序，将这些程序（v0,v1,v2等等）均写入prompt，再在prompt的末尾加上需要新写的一个函数头（v3:#基于v0,v1,v2进行改进）,最后将这个prompt喂给预训练过的大模型进行交叉和变异，最终形成一个新的程序。
对于岛屿进化算法，则是定期对岛屿进行排名，并且删除排名在最后一半的岛屿中的全部程序，然后从幸存的岛屿中克隆最佳程序给排名靠后的岛屿，以此保证进化的高效和信息流通。
```
Crucially, we let information flow between the islands by periodically discarding the programs in the worst half of the islands (corresponding to the ones whose best individuals have the lowest scores). We replace the programs in those islands with a new population, initialized by cloning one of the best individuals from the surviving islands.

—————— 《《Mathematical Discoveries from program search with large language models》》
```

sample prompt:
```python
"""Finds large cap sets.  
  
On every iteration, improve priority_v1 over the priority_vX methods from previous iterations.  
Make only small changes.  
Try to make the code short.  
"""  
import itertools  
  
import numpy as np  
  
import funsearch  
  
  
@funsearch.run  
def priority_v0(el: tuple[int, ...], n: int) -> float:  
  """Returns the priority with which we want to add `element` to the cap set.  
  el is a tuple of length n with values 0-2.  
  """  
  return 0.0  
  
  
def priority_v1(el: tuple[int, ...], n: int) -> float:  
  """Improved version of `priority_v0`."""
```

sample reponse:
```
```python  
def priority_v1(el: tuple[int, ...], n: int) -> float:  
  """Improved version of `priority_v0`."""  
  # Prioritize elements with fewer zero entries (more non-zero values)  
  return sum(1 for x in el if x != 0)  
```

针对LLM进化后的程序及进行evaluation评估，评估通过后的进化程序进入program database
![Pasted image 20260401212507.png](./assets/media/images/r1_2.png)
### 二、针对个体数据进行拟合与进化
我们希望将FunSearch应用到一下的一种情况：
由于人在本质上是相似的，因此人在做决定的时候会收到相同因素的一些影响，这是由人的共性决定的。又由于人分别具有自己的特性，因此在这些相同的因素上又会有一些细微的差别。
建模的方向：群体决策时收到相同的因素影响，可以用同一个模型建模出来。由于每个人的个性，模型中的参数会因此变化。e.g. 大部分人做决策的时候都会考虑到风险问题，但是有些人时risk-seeking的人，有一些人时risk-avoiding的人。

![Pasted image 20260401212629.png](./assets/media/images/r1_3.png)
我们在每个program进化后做evaluation的环节增加了评分部分：
对于每一个被试个体的数据做交叉验证评分，然后加权平均每一个个体的分数得到程序拟合的总分。高分程序更有可能参与程序的后续进化。

**具体的评分函数：**
$$
\Omega = \exp\left( \frac{2}{MT} \sum_{i,t} \log p(c_{it}) \right)
$$
输出示例：
![Pasted image 20260401214144.png](./assets/media/images/r1_4.png)

### 三、总结
行为建模特别的一点在于当一个模型出现特别的参数计算的时候，我们容易去解释它，将参数套入人的行为。而手工构建模型的时候想到这种行为进行建模却是很难的。

例如大鼠数据集的baseline模型**RHG**，r是奖励，人会倾向于选择能够获得更高奖励的行为，h是习惯，尽管某些行为无法获得更高的奖励但因为我们的习惯还是会选择它，g是赌徒谬论，当一个行为很久没有被选择的时候，我们会认为它该出现了，然后选择它。

感觉有意思是因为确实发现这样的模型解释确实能够很大程度上适配我的行为，某些烟不是最好抽的但由于习惯抽所以我还是会选它；有更近更快的路但我喜欢走我更熟悉的路；蒙最后一个选择题的时候喜欢盲狙看那个选项出现的少然后选他它。在计算选择概率的时候这篇文章的模型给概率设定了一个上限，无论softmax得到的结果多么接近于1都不可能等于1。

背后的逻辑在于无论一个选项能够获得多高的奖励人选择它的概率也不是百分百，因为人可能会点错，就好像明明知道答案却手滑的选择题，或者想要探索其他的选项。


又比如，在计算人类的选择概率的时候，LLM进化得到的程序给概率设定了一个上限，无论softmax得到的结果多么接近于1都不可能等于1。

**背后的逻辑在于无论一个选项能够获得多高的奖励人选择它的概率也不是百分百，因为人可能会点错，就好像明明知道答案却手滑的选择题，或者想要探索其他的选项。**